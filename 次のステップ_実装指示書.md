# FLAT-UP GYM AI診断機能 - 次のステップ実装指示書

## 📌 実装状況

**完了項目（フェーズ0 - MVP）**:
- ✅ AI診断トップページ（`/ai`）
- ✅ 食事診断フォーム（`/ai/meal`）
- ✅ フォーム診断ページ（`/ai/form`）
- ✅ 適性診断ページ（`/ai/aptitude`）
- ✅ Next.js API Routes（`/api/diagnostic/start`, `/api/line/webhook`）
- ✅ Dify連携ユーティリティ（`lib/dify.ts`）
- ✅ LINE連携ユーティリティ（`lib/line.ts`）
- ✅ 型定義（`lib/types/diagnostic.ts`）
- ✅ ナビゲーションに「🤖 AI診断」リンク追加

**未完了項目（フェーズ1以降）**:
- ⏳ 環境変数の設定
- ⏳ Difyワークフローの作成
- ⏳ LINE Webhook設定
- ⏳ Firestore実装（オプション）
- ⏳ テスト・動作確認

---

## 🎯 次の実装タスク

### タスク1: 環境変数の設定（必須）

**所要時間**: 10分

**手順**:

1. プロジェクトルートに `.env.local` ファイルを作成

2. 以下の環境変数を設定：

```bash
# Dify Configuration
DIFY_TRIGGER_URL=https://api.dify.ai/v1
DIFY_API_KEY=your_dify_api_key_here
DIFY_WEBHOOK_SECRET=your_webhook_secret_here
DIFY_WORKFLOW_MEAL_ID=meal_diagnosis
DIFY_WORKFLOW_FORM_ID=form_evaluation
DIFY_WORKFLOW_APTITUDE_ID=aptitude_test

# LINE Messaging API Configuration
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token_here
LINE_CHANNEL_SECRET=your_line_channel_secret_here
```

3. 実際の値を取得して設定（後続タスクで説明）

**確認方法**:
```bash
# 開発サーバー起動時に環境変数が読み込まれているか確認
npm run dev
```

---

### タスク2: Difyワークフローの作成（必須）

**所要時間**: 1-2時間

**手順**:

#### 2-1. Difyアカウントでログイン

1. Difyにログイン: https://dify.ai/
2. ワークスペースを選択または作成

#### 2-2. 食事診断ワークフロー作成

1. **ワークフロー作成**
   - 「ワークフロー」→「新規作成」
   - ワークフロー名: `meal_diagnosis`

2. **入力変数の定義**
   ```
   age (Number)
   sex (String: male/female/other)
   height_cm (Number)
   weight_kg (Number)
   activity_level (String: sedentary/light/moderate/active/very_active)
   goals (String: lose_weight/maintain/gain_weight/build_muscle)
   diagnostic_type (String: meal)
   ```

3. **AIノードの設定**
   - LLMノードを追加
   - プロンプト例:
   ```
   あなたは栄養士とパーソナルトレーナーの資格を持つ専門家です。
   以下の情報を基に、最適な食事プランを提案してください。
   
   年齢: {{age}}歳
   性別: {{sex}}
   身長: {{height_cm}}cm
   体重: {{weight_kg}}kg
   活動レベル: {{activity_level}}
   目標: {{goals}}
   
   以下の形式で回答してください：
   - 適正カロリー
   - 栄養バランスの推奨
   - 具体的な食事プラン
   - 注意点
   ```

4. **出力変数の設定**
   - 出力スキーマを定義:
   ```json
   {
     "diagnosis_id": "string",
     "summary": "string",
     "recommendations": [
       {
         "type": "string",
         "text": "string"
       }
     ],
     "confidence": "number",
     "raw_text": "string"
   }
   ```

5. **Webhookトリガーの設定**
   - 「トリガー」→「Webhook」を追加
   - Webhook URLをコピー
   - Secretを設定（任意）

6. **公開・保存**
   - ワークフローを保存
   - 公開（必要に応じて）

#### 2-3. フォーム診断ワークフロー作成

同様の手順で以下を作成：

- ワークフロー名: `form_evaluation`
- 入力変数:
  ```
  description (String, optional)
  technique (String: punch/kick/guard/other)
  videoUrl (String, optional)
  diagnostic_type (String: form)
  ```
- プロンプト例:
  ```
  あなたは格闘技の専門コーチです。
  以下の情報を基に、フォームの改善点を提案してください。
  
  技の種類: {{technique}}
  説明: {{description}}
  動画URL: {{videoUrl}}
  
  以下の形式で回答してください：
  - フォームの良い点
  - 改善すべき点
  - 具体的な改善方法
  - 練習メニューの提案
  ```

#### 2-4. 適性診断ワークフロー作成

同様の手順で以下を作成：

- ワークフロー名: `aptitude_test`
- 入力変数:
  ```
  totalScore (Number)
  answers (Array)
  diagnostic_type (String: aptitude)
  ```
- プロンプト例:
  ```
  あなたは格闘技の適性を診断する専門家です。
  以下の情報を基に、最適な格闘技スタイルを提案してください。
  
  スコア: {{totalScore}}
  回答内容: {{answers}}
  
  以下の形式で回答してください：
  - 適性タイプ
  - おすすめの格闘技スタイル
  - 理由
  - 具体的なプログラム提案
  ```

#### 2-5. 環境変数に設定

各ワークフローのWebhook URLとIDを `.env.local` に設定：

```bash
DIFY_TRIGGER_URL=https://api.dify.ai/v1  # または実際のDifyインスタンスURL
DIFY_WORKFLOW_MEAL_ID=meal_diagnosis
DIFY_WORKFLOW_FORM_ID=form_evaluation
DIFY_WORKFLOW_APTITUDE_ID=aptitude_test
```

**確認方法**:
- Difyのワークフロー一覧で各ワークフローが公開されているか確認
- Webhook URLが正しく設定されているか確認

---

### タスク3: LINE Webhook設定（必須）

**所要時間**: 30分

**手順**:

#### 3-1. LINE Developers Consoleにアクセス

1. https://developers.line.biz/console/ にログイン
2. プロバイダーを選択
3. チャネルを選択（または新規作成）

#### 3-2. Channel Access Tokenを取得

1. 「Messaging API」タブを開く
2. 「Channel access token」セクションで「Issue」をクリック
3. トークンをコピー（再表示不可のため注意）
4. `.env.local` に設定:
   ```bash
   LINE_CHANNEL_ACCESS_TOKEN=your_token_here
   ```

#### 3-3. Channel Secretを取得

1. 「Basic settings」タブを開く
2. 「Channel secret」をコピー
3. `.env.local` に設定:
   ```bash
   LINE_CHANNEL_SECRET=your_secret_here
   ```

#### 3-4. Webhook URLを設定

1. 「Messaging API」タブを開く
2. 「Webhook URL」に以下を設定:
   ```
   https://your-domain.com/api/line/webhook
   ```
   ※ 開発環境の場合: `https://your-ngrok-url.ngrok.io/api/line/webhook` など

3. 「Webhook settings」で「Use webhook」を有効化

#### 3-5. Webhook検証

1. 「Verify」ボタンをクリック
2. 「Success」と表示されればOK

**確認方法**:
- LINE公式アカウントにメッセージを送信
- Webhookが正しく受信されているか確認（サーバーログ）

---

### タスク4: Next.js設定の確認（必須）

**所要時間**: 5分

**手順**:

1. `next.config.mjs` を開く

2. 静的エクスポート設定を確認：

```javascript
// ❌ API Routesが動作しない設定
const nextConfig = {
  output: 'export',  // この行があるとAPI Routesが動作しない
  // ...
};

// ✅ API Routesを使用する場合
const nextConfig = {
  // output: 'export', // コメントアウトまたは削除
  images: {
    unoptimized: true,
    remotePatterns: [
      { protocol: 'https', hostname: '**' },
    ],
  },
};
```

3. 静的エクスポートが必要な場合（Netlify等）:
   - Cloud Functionsまたは別のバックエンドサービスを使用
   - Next.js API Routesの代わりに実装

**確認方法**:
```bash
npm run dev
# http://localhost:3000/api/diagnostic/start にアクセス
# エラーが表示されなければOK
```

---

### タスク5: 動作確認テスト（必須）

**所要時間**: 30分

**テスト項目**:

#### 5-1. フロントエンドテスト

1. **AI診断トップページ**
   - URL: `http://localhost:3000/ai`
   - 3つの診断カードが表示されるか確認
   - 各カードのリンクが正しく動作するか確認

2. **食事診断フォーム**
   - URL: `http://localhost:3000/ai/meal`
   - フォーム入力が正常に動作するか確認
   - 送信ボタンが動作するか確認

3. **フォーム診断ページ**
   - URL: `http://localhost:3000/ai/form`
   - フォーム入力が正常に動作するか確認

4. **適性診断ページ**
   - URL: `http://localhost:3000/ai/aptitude`
   - クイズが正常に動作するか確認
   - 結果表示が正常に動作するか確認

#### 5-2. APIテスト

1. **診断APIテスト**
   ```bash
   curl -X POST http://localhost:3000/api/diagnostic/start \
     -H "Content-Type: application/json" \
     -d '{
       "type": "meal",
       "payload": {
         "age": 30,
         "sex": "female",
         "height_cm": 165,
         "weight_kg": 60,
         "activity_level": "light",
         "goals": "lose_weight"
       }
     }'
   ```

2. **LINE Webhookテスト**
   - LINE Developers Consoleの「Webhook settings」で「Verify」を実行
   - または、LINE公式アカウントにメッセージを送信して確認

#### 5-3. 統合テスト

1. **食事診断フロー**
   - フォーム入力 → 送信 → Dify呼び出し → LINE送信（オプション）

2. **LINE Webhookフロー**
   - LINEメッセージ送信 → Webhook受信 → 返信送信

---

### タスク6: Firestore実装（オプション）

**所要時間**: 1-2時間

**手順**:

1. **Firebase Admin SDKのインストール**
   ```bash
   npm install firebase-admin
   ```

2. **Firebase初期化**
   `lib/firebase-admin.ts` を作成:
   ```typescript
   import { initializeApp, getApps, cert } from 'firebase-admin/app';
   import { getFirestore } from 'firebase-admin/firestore';

   if (!getApps().length) {
     initializeApp({
       credential: cert({
         projectId: process.env.FIREBASE_PROJECT_ID,
         clientEmail: process.env.FIREBASE_CLIENT_EMAIL,
         privateKey: process.env.FIREBASE_PRIVATE_KEY?.replace(/\\n/g, '\n'),
       }),
     });
   }

   export const db = getFirestore();
   ```

3. **`lib/firestore.ts` の実装**
   - `saveDiagnosticRecord()` を実装
   - `getDiagnosticRecord()` を実装
   - `updateDiagnosticStatus()` を実装
   - `getUserDiagnostics()` を実装

4. **セキュリティルール設定**
   Firestore Consoleでセキュリティルールを設定

---

## 🐛 トラブルシューティング

### 問題1: API Routesが動作しない

**原因**: `next.config.mjs` で `output: 'export'` が設定されている

**解決策**: `output: 'export'` をコメントアウトまたは削除

---

### 問題2: Dify API呼び出しが失敗する

**原因**: 
- 環境変数が設定されていない
- Webhook URLが間違っている
- APIキーが無効

**解決策**:
1. `.env.local` の環境変数を確認
2. Difyのワークフローが公開されているか確認
3. Webhook URLが正しいか確認

---

### 問題3: LINE Webhookが動作しない

**原因**:
- Webhook URLが設定されていない
- 署名検証が失敗している
- Channel Access Tokenが無効

**解決策**:
1. LINE Developers ConsoleでWebhook URLを確認
2. Channel Secretが正しく設定されているか確認
3. サーバーログでエラー内容を確認

---

### 問題4: 環境変数が読み込まれない

**原因**: 
- `.env.local` がプロジェクトルートにない
- 環境変数名が間違っている
- 開発サーバーを再起動していない

**解決策**:
1. `.env.local` がプロジェクトルートにあるか確認
2. 環境変数名を確認（大文字小文字に注意）
3. 開発サーバーを再起動: `npm run dev`

---

## 📋 実装チェックリスト

実装完了後、以下を確認してください：

- [ ] `.env.local` に環境変数を設定
- [ ] Difyで3つのワークフローを作成
- [ ] DifyワークフローのWebhook URLを取得
- [ ] LINE Developers ConsoleでWebhookを設定
- [ ] LINE Channel Access TokenとSecretを取得
- [ ] `next.config.mjs` でAPI Routesが有効か確認
- [ ] 開発サーバーで動作確認（`npm run dev`）
- [ ] 各診断フォームから送信テスト
- [ ] LINE webhookの動作確認
- [ ] Firestore実装（必要に応じて）

---

## 📞 サポート

実装中に問題が発生した場合：

1. **エラーログを確認**
   - ブラウザコンソール（F12）
   - サーバーログ（ターミナル）

2. **環境変数を確認**
   - `.env.local` が正しく設定されているか
   - 環境変数名が正しいか

3. **API動作を確認**
   - Difyワークフローが公開されているか
   - LINE Webhookが有効化されているか

---

## 🎉 完了後の次のステップ

実装が完了したら、以下を検討してください：

1. **フェーズ1（UX改善）**
   - エラーハンドリングの強化
   - ローディング状態の改善
   - レスポンシブデザインの最適化

2. **フェーズ2（機能拡張）**
   - PDFレポート生成
   - 診断履歴の保存・表示
   - A/Bテストの実装

3. **運用・監視**
   - ログ監視の設定
   - エラー通知の設定
   - パフォーマンス監視

---

**実装日**: 2024年11月28日  
**バージョン**: MVP (フェーズ0)  
**次のフェーズ**: フェーズ1（UX改善＋安定化）

以上が次のステップの実装指示書です。順番に実装を進めてください。

